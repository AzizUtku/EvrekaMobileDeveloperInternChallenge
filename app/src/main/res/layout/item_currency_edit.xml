<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/currency_edit_rlt_main"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#fff"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/row_padding_vertical"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingBottom="@dimen/row_padding_vertical">

    <RelativeLayout
        android:id="@+id/currency_edit_rlt_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp">

        <Button
            android:id="@+id/currency_edit_btn_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_btn_only_stroke_selector"
            android:elevation="0dp"
            android:fontFamily="@font/avenirnext_medium"
            android:minHeight="35dp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/remove"
            android:textColor="#000"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/currency_edit_txt_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_marginStart="15dp"
            android:fontFamily="@font/avenirnext_regular"
            android:text="Turkish Lira"
            android:textColor="@android:color/black"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/currency_edit_txt_code"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/currency_edit_txt_name"
            android:layout_alignStart="@+id/currency_edit_txt_name"
            android:layout_marginTop="1dp"
            android:fontFamily="@font/avenirnext_regular"
            android:text="TRY"
            android:textColor="#8B000000"
            android:textSize="14sp" />

    </RelativeLayout>

</RelativeLayout>